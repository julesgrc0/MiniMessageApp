import { MessageRenderPipe } from './message-render.pipe';

describe('MessageRenderPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageRenderPipe();
    expect(pipe).toBeTruthy();
  });
});
